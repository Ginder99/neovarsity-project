name: Deploy auth-service

on:
  push:
    branches: [release]
    paths:
      - 'auth-service/**'
      - '.github/workflows/auth-service-deploy.yml'

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy-service.yml
    with:
      service_name: auth-service
      service_path: auth-service
      cloudsql_instance: scalar-neovarsity-vms:us-central1:vms-db-instance
      secrets_mapping: "DB_PASSWORD=auth-db-user-password:latest,JWT_SECRET=jwt-secret:latest"
      runtime_service_account: auth-service-runtime@scalar-neovarsity-vms.iam.gserviceaccount.com
    secrets:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_PROJECT_NUMBER: ${{ secrets.GCP_PROJECT_NUMBER }}
